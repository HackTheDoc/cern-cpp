var x : int
var flag = false
var c : char = itoc(1)

func calc() : int {
    return (64 / 2 + 2) * 2
}

func main() : int {
    if (!flag) {
        x = calc()
        println(x+ctoi(c))
    }
    else {
        println(42)
    }
    
    return 0
}
